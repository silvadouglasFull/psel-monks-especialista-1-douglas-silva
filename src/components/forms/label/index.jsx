import styled from "styled-components";

/**
 * `Label` is a styled component used to render a label element with no default styling.
 * It can be used to style labels in forms or other elements.
 * 
 * This component does not apply any specific styles by default, but it can be customized further 
 * by adding custom CSS properties or by passing additional props.
 *
 * @component
 * @example
 * <Label htmlFor="username">Username</Label>
 * 
 * @param {Object} props - The props for the component.
 * @returns {JSX.Element} The rendered `label` component with the applied styles.
 */
const Label = styled.label``;

export { Label };
