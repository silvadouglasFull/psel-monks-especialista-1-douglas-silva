import styled from "styled-components";

/**
 * Styled component for displaying an image.
 * The image has a maximum width and height to maintain consistency in design.
 */
const ImageContainer = styled.img`
  max-width: 2.1875rem; /* Sets the maximum width */
  max-height: 2.1875rem; /* Sets the maximum height */
  margin: 0.5rem; /* Adds spacing around the image */
`;

export { ImageContainer };
